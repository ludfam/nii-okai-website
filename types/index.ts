export type Children = {
    children: React.ReactNode
}

export type HeaderSocial = {
    label: any
    link: string
}

export type NewsPost = {
    _id: string
    title: string
    img: string
    link: string
}